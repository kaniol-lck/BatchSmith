# 变更记录

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 项目按 P1–P8 阶段推进，见 [`docs/项目阶段规划.md`](docs/项目阶段规划.md)。
> **0.x 期间不保证接口稳定**：预设格式、DSL 语义、CLI 参数都可能变。

## [未发布]

### 新增

- **DSL 新增「文件名」类工具函数**（原有的 `ext` / `stem` 只负责拆，这几个负责改）：
  - `set_ext(p, e)` —— **换**扩展名。第二参数带不带点都认，给空串等于去掉扩展名；
    本来没有扩展名就直接接上（`a` + `mkv` → `a.mkv`，不会变成 `a..mkv`）。
  - `add_ext(p, e)` —— 在**末尾追加**一层（`a.mkv` → `a.mkv.bak`），备份那种场合用。
  - `add_suffix(p, s)` —— 插在**扩展名之前**（`a.mkv` → `a_final.mkv`），
    「给一整批文件加同一个标记」最常用的动作。用 `replace` 做不到这件事：
    它会把**目录名里**的同名字面串一起换掉。
  - `safe_name(s[, repl])` —— 把一段文本变成**能真正落盘**的文件名。做三件不做就会
    静默出错的事：换掉 Windows 非法字符（`<>:"/\|?*` 与控制字符）、去掉结尾的点与
    空格（Windows 会**静默截断**它们，截完与计划里写的名字不一致，最难查）、
    给设备名（`con` / `nul` / `com1`…）补下划线。**整串当作名字，不拆路径** ——
    输入是标题/字段这类名字；要保留目录就自己拼 `join(dirname(p), safe_name(basename(p)))`。
- **`ext` / `stem` 的「什么算扩展名」判定抽成一份共享实现**：以前这两个函数各自抄了
  一遍 `lastIndexOf` 的算法，加第三个函数时必然分叉。现在只有 `split_name()` 一处
  定义（最后一个点、必须在最后一个 `/` 之后、不能是文件名首字符），
  `set_ext` / `add_ext` / `add_suffix` 全部依赖它。
- **DSL 速查表与帮助入口**：界面「帮助 → DSL 语法与函数速查」（**F1**）与
  `bs cheatsheet` 显示的是**同一份**内容（都由 `core::dsl::cheatsheet_text()` 渲染）——
  模板写法、区段里可用的名字、行数与展开规则、全部 33 个工具函数（按列表/组合/生成/
  文本/路径/文件名/类型分组）、沙箱里可用与不可用的 Lua 能力、四个易错点，
  以及 41 条**带实际输出**的示例。
- **速查表按数据定义**（`dsl/cheatsheet.hpp` 的 `helper_docs()` / `cheat_examples()`），
  渲染文本由数据生成，测试遍历数据核对：函数名与沙箱实际注册名**双向**比对、
  每条示例真的求值一遍并逐行核对声明输出。于是「文档说过时的话」这类问题会在 CI 就被挡下。
- `helper::registered_names()`：暴露实际注册的工具函数名，供上面的交叉校验使用。
- **列表可以绑定文件夹**（P3 的第一批）：每列在「手输」与「文件夹」两种来源之间切换，
  **列名与编号不随切换改变**（表达式里的 `$list1[i]$` 不会因为换来源而指向别处）。
  文件夹来源支持 glob 过滤、递归、含目录、含隐藏项；条目按**自然序**读入
  （`第2话` 在 `第10话` 之前），值为**相对该文件夹**的路径（`/` 分隔，要绝对路径就
  `join(根, list1[i])`）。界面上把文件夹**拖到列上**即可绑定 —— 与点「…」选择、
  以及测试走的是同一个方法，所以三条入口没有行为差别。
- **`core::list::scan_directory()`**：文件夹取数与 glob 匹配。glob 支持
  `*` / `?` / `[abc]` / `[!abc]` / `\` 转义；**不跨目录分隔符**（模式只作用于条目名，
  目录层级由「递归」开关表达），默认**大小写不敏感**（Windows/macOS 的文件系统不敏感，
  同一份规格在三个平台必须给出同一个答案）。
- **CLI 的文件夹列表**：`--list '名=@dir:<文件夹>[;filter=<glob>][;recursive=1][;dirs=1]…'`。
  路径不通**直接报错并返回非零退出码**，不给空列表 —— 空列表会让"批量操作什么都没做"
  看起来像正常结果。
- 速查表新增「三、列表从哪来（手输 / 绑定文件夹）」一节，文本 / HTML / CHM 三形态同时生效。
- **预设：保存 / 加载 / 管理**（P3 的预设部分）。把"当前的列表 + 表达式"存成 `.toml`；
  界面「文件」菜单可新建 / 打开（`Ctrl+O`）/ 保存（`Ctrl+S`）/ 另存为 / 管理预设，
  启动时也能直接带上：`batchsmith 我的预设.toml`（位置参数与 `--preset` 都行）。
- **`core::preset`**：预设 schema 与 TOML 读写。读用 `toml++`，**写是手写的**（要保留
  注释与排版 —— 预设文件是给人看、给人改的）；字符串一律写成 TOML **字面字符串**，
  于是 Windows 路径的反斜杠与模板里的 `$`、`\d` 都不用转义。
- **路径槽位 `${input}`**：预设里**不存绝对路径**。保存时文件夹路径自动槽位化
  （同一个路径共用一个槽位），实际路径写进旁边的 `<预设名>.local.toml` —— 那是本机信息，
  分享时不要带。于是预设文件可以进版本控制、可以发给别人。
- 加载时**未绑定的槽位会明确说出来**（附带槽位名），而不是静默给一个空列；
  把文件夹拖到那一列上就自动绑好，保存时记住。
- 「管理预设」对话框列出用户预设目录里的全部预设：可打开 / 重命名（**文件名与预设名一起改**，
  免得两个名字不一致）/ 删除（连伴生绑定文件一起）。**读不动的预设也会列出来并标红**，
  否则用户只会在"打开预设"里撞墙，却找不到是哪个文件的问题。
- 窗口标题显示当前预设名与未保存标记（`*`），关闭前会问一句；「预设」菜单记住最近 8 个。
- **还不支持的写法一律明确报错，不静默忽略**：`source.kind` 只认 `manual` / `dir`
  （写 `snapshot` 会提示"本版本还不支持"）、`sort` 只认 `natural`、`output.mode` 只认 `rename`。
  写了没实现的字段却看起来"配了就能用"，是比报错更糟的状态。
- 速查表新增「九、把当前配置存成预设」（含文件结构示例），三形态同时生效。
- **保存预设不再问路径**：没存过的预设按 `Ctrl+S` 直接落进用户预设目录
  （文件名取预设名，没名字用 `未命名预设`，重名自动加序号，**不覆盖已有的**）。
  要挑地方是「另存为」的事 —— 那个动作本身就表达了"我要放到别处"。
- **「预设」菜单**：顶层菜单「预设(&P)」里**直接列出最近用过的 8 个预设**（不再套子菜单），
  点一下就切过去，当前打开的那个带勾；后面是「管理预设…」与「打开预设文件夹」
  （从「文件」菜单移过来，避免两处重复）。菜单弹出前会重建一次，
  文件被外部删掉后不会留下"点了必然失败"的条目。
- **「管理预设 → 创建快捷方式」**：生成一个带着 `--preset "<预设文件>"` 启动的快捷方式，
  双击即用某套配置打开 BatchSmith。Windows 走 `IShellLink` + `IPersistFile`（`.lnk`），
  Linux 写 `.desktop`（并置可执行位），macOS 写 `.command`。默认建到桌面，
  桌面拿不到就退回预设目录；重名加序号，不覆盖。
  建完会**读回来核对**（程序路径与 `--preset` 参数对不对得上）——
  建一个指向空气的快捷方式比不建更糟，而用户双击只会看到"打开了个空白窗口"。
- `core::preset::unique_file_path()`：默认文件名/快捷方式名的净化与去重
  （非法字符换下划线、去掉结尾的点与空格、拒绝 `.local` 结尾——那是绑定文件的后缀，
  用它当预设名会导致文件下次读不出来；重名依次试 `名字 2.ext`）。
  放在 core 而不是各调用点各写一遍：预设与快捷方式都要这套，分家必然漂移。
- **`regex` 的第三个参数**：`regex(s, pattern[, n])` —— 给了 `n` 就直接取捕获列表的第 n 项，
  省掉一层下标；越界与不匹配都给**空串**（与列表越界的约定一致，不会拿到 nil）。
  `regex(名字, [[第(\d+)话]], 1)` 现在是取话号最顺手的写法。
- **`resub`：正则全局替换**，`to` 里可用 `\1`…`\9` 引用捕获组。
- **管理预设新增三列与三个动作**：列变成
  **预设 | 列表 | 输出表达式 | 备注 | 修改时间 | 文件** —— 「输出表达式」直接摊出来，
  「这个预设到底会做什么」一眼可见（空模板显示「（空）」）。
  - **备注**（`[preset] note`）：一句给人看的话，用「备注…」改。它**存在预设文件里**，
    会跟着预设一起分享出去。
  - **图标**（可选）：用「图标…」挑、画在预设名旁边，「清除图标」去掉（没设过时是灰的）。
    图标是**本机路径**，所以写进 `.local.toml` 而**不进预设文件** —— 预设照样能干净地发出去。
    建快捷方式时用它（Windows `SetIconLocation` / Linux `.desktop` 的 `Icon=`；
    macOS 的 `.command` 没有图标位置，会忽略）。
- **「创建快捷方式…」改成先问放哪**：弹一个目录选择框（默认停在桌面，但不替用户决定 ——
  快捷方式经常要放进某个项目文件夹或启动器目录）。
- `core::preset::LocalSettings`：把 `.local.toml` 的内容从"只有槽位绑定"升级成
  **槽位绑定 + 快捷方式图标**。两者在**同一个文件**里，所以读写必须一起 ——
  `save_bindings()` 现在是"先读回来、只换绑定那一节"，不会把图标抹掉
  （分成两套 API 直接覆盖整个文件，就会出现"设了图标，改一次路径绑定就没了"这种
  用户根本联想不到的问题）。

- **`bs plan <预设.toml>`：先把计划算出来再动手**（P3 的最后一块）。
  一行一个"源 → 新名字"，把这次批量操作会做什么连同冲突一起摊出来，
  **只读、零副作用** —— 真正落盘的 `apply` 属 Phase 4，而计划是它之前必须过的一步。
  - **行与文件是绑死的**：每行同时给出相对名与绝对路径，条目按自然序
    （`file2` 在 `file10` 之前）—— 用户核对"第 7 行是不是我想的那个文件"才有意义。
  - **问题当场标出来，不留到执行阶段**：两条输出同名（并指出与哪一行撞了）、
    目标已存在且不是这批里的文件、目标跑出绑定文件夹（绝对路径 / `..` / 驱动器相对路径）、
    同一个文件被多行用到、求值结果是空串、这一行没有对应的文件。
    「目标与源同名」单独算一类（**不算问题**）：本来就对，不该让 dry-run 报失败。
  - **批内互换与"仅改大小写"会被认出来，但标成可执行**：它们在执行时要借临时名
    （Phase 4 的两阶段重命名），计划里用一条提醒说清"这些现在只是列出来"。
  - `--bind 槽位=路径` 覆盖预设旁边 `.local.toml` 里的绑定（命令行写的是这次明确要求的，
    文件里那份是上次留下的）；`--json` 输出机器可读的计划（`state` 是稳定的 ASCII 词）。
    退出码分开：`0` 全部可执行 · `1` 有不可执行的行 · `2` 用法错 · `3` 计划没算出来。
  - **不确定的就报错，不猜**：预设里有**两个**文件夹来源的列表时拒绝生成
    （"哪一列是被改名的那些文件"是歧义，猜错的后果是把文件改成别的名字）；
    模板没有逐行产名字时也拒绝（没用 `i` / `rows` 所以整批只求值一次，
    或某个区段行展开成多行 —— 那是 stdout 模式的用法，不是重命名）。
- **`core::plan`**：`Plan` 是**纯数据**（可序列化、可 diff、可单测），
  `build_plan()` 是**纯函数**（只读预设与文件系统）。于是"预览与实际执行一致"
  变成一条可检查的性质、而不是靠自觉 —— 单测里两条断言直接钉住它：
  算一次计划之后目录快照（条目名 + 大小 + mtime）逐项不变；
  含 `rand()` 的预设两次算出的结果**逐字相同**（沙箱的随机源有固定种子）。
- **`dsl::BatchResult::row_count`**：把**输入行数**单独暴露出来
  （`rows.size()` 是输出行数，行展开时两者并不相等）。`bs plan` 靠它判断
  "输出行 ↔ 输入行"是否一一对应，而不是把"有 `Ignore` 取最短、否则取最长"
  这条规则在第二个地方再抄一遍。

### 变更

- **⚠️ 破坏性：`replace` 从「正则替换」改成「字面替换」**，正则版改名为 `resub`。
  理由：批量改名时绝大多数要换的是普通子串，而 `.`、`(`、`[` 在正则里都有特殊含义 ——
  让 `replace` 认正则，等于要求用户每写一个字面串都先想"这里有没有元字符"。
  **老预设里 `replace(x, "a+", "b")` 的含义会从"一个或多个 a"变成"字面 a+"**，
  结果通常是不再匹配（不再替换）。要旧行为就把 `replace` 改成 `resub`。
  另外 `replace` 会拒绝空串（字面语义下那是"每个字符之间都插一段"）。
- 输入框的占位提示更新为当前语法：
  `$ 里写 Lua 表达式，例如 mv $list1[i]$ out/$list2[i]$；按 F1 查看语法与函数`。
  （原来的提示还是 0.2.0 时代只认列表名的写法。）
- `bs` 的版本横幅改走 **stderr**：stdout 只保留真正的输出，
  这样 `bs eval … | wc -l` 不会莫名多出一行 —— 横幅属于诊断信息，不属于数据。
- 帮助对话框做成独立控件 `gui/help/CheatsheetDialog`，可被离屏测试直接构造验证。
- 帮助窗口改为**非模态**（`show()` 而非 `open()`）：看帮助的典型场景就是
  "照着示例改表达式"，模态窗口会让这件事做不成。重复按 F1 只把已有窗口提到前面。
- 帮助内容改为**按章节渲染 HTML**（`cheatsheet_html()`），窗口左侧加了章节跳转下拉，
  并可一键「另存为 HTML」导出成单文件手册。
- **CHM 帮助手册**：`packaging/make-chm.sh` 把速查表编译成 CHM（Windows 原生帮助，
  双击即看、带左侧目录树），目录树由 core 数据生成（`cheatsheet_hhc()`）。
  Windows 发布包随附 CHM，三平台包都随附 HTML 手册 —— 手册离线可用、随版本走。
- **`packaging/fetch-hhc.sh`**：自动获取 `hhc.exe`。微软官方下载已下线、
  winget 与 MSYS2 仓库里都没有，但安装包是「自解压 CAB」结构，所以脚本直接
  从 CAB 里取出 `hhc.exe` / `itcc.dll` / `hha.dll` —— **不跑安装器、不改注册表、
  不需要管理员权限**，CI 上也能用。
- `bs cheatsheet --hhc`：输出 CHM 的目录文件（`.hhc`）；
  `--html` 同前（可重定向成单文件手册）。
- 文档：`docs/界面说明.md` 增加「帮助」一章与帮助截图，测试数字更新为当前值。
- 列表列宽度 190 → 218：文件夹模式要放得下一行路径输入。
- 速查表的章节标题改为**按锚点**取，不再按数组下标：在中间插入一章时，下标会各自错位成
  「标题配错正文」，而锚点写错只会得到空标题（测试能抓住）。漏写正文时渲染器会留下一句
  醒目占位，也有测试盯着。
- `docs/界面说明.md` 增加「列表来源：一列，两种模式」一章，记录上述几条明确的选择
  （相对路径、固定自然序、过滤不跨目录、**过滤不阻挡递归**、失败保留上次结果）。
- 截图夹具（`tests/gui/ui_shot.cpp`）改成绑定一个真实临时文件夹，并在生成时往 stderr
  打一行自证（各列模式/项数/状态、输出行数）—— 截图是在无图形环境里生成的，
  生成者看不到图，看图的人又未必知道"本该有什么"。
- 示例预设 `presets/example-rename.toml` 更新成**当前能用的写法**（原来的
  `kind = "snapshot"` 尚未实现），并加了一条测试盯着它 —— 示例文件会随时间腐坏，得有人管。
- `ListSourcePanel::setSources()`：按预设重建列时**先摘掉旧列的父子关系再 `deleteLater()`**。
  删除是延后的，不摘的话紧接着按名字找 `list1` 会先找到"等着被删的旧列"，
  于是刚装好的内容看起来像没生效（测试当场抓到了这一条）。
- GUI 测试入口（`tests/gui/main.cpp`）改用独立的组织/应用名并打开 Qt 的测试模式：
  预设相关用例会写"用户预设目录"与「最近用过」，不隔离的话跑一次测试就会污染
  开发机上真实的配置。「创建快捷方式」的用例同理：目标目录由
  `PresetManagerDialog::setShortcutDirectory()` 指到临时目录，不往真实桌面上放东西。
- 「文件」菜单只留文件本身的动作（新建 / 打开 / 保存 / 另存为 / 退出），
  预设的挑选与管理移到新的「预设」菜单 —— 理由见 `docs/界面说明.md`：
  `文件 → 保存` 是惯例位置，而"常用的几套配置"是像书签一样越用越长的一列，
  混在一起时长起来的那段会把"保存"挤到看不见的地方。
- 截图夹具新增 `BATCHSMITH_PRESET_SHOT`：把「管理预设」对话框渲染成 PNG
  （会临时往预设目录写几份演示预设，含一份**故意写坏的** —— 不然"坏文件也要列出来并标红"
  在图上不成立），生成时同样往 stderr 打一行自证。
- **Windows 内存诊断通道**（CI 的 `windows-heapcheck` 作业，不影响发布）：
  Windows 的单元测试自引入 Lua 沙箱起一直以 `Exit code 0xc0000374`
  （`STATUS_HEAP_CORRUPTION`）收场，而 Linux / macOS / 本机 MinGW 全绿 —— 说明有一处
  内存越界，只在 MSVC 的堆布局下暴露。Release 用的是发布版 CRT（分配里没有守卫字节），
  越界写会悄悄发生，直到某块内存被释放才由 OS 发现，那时崩溃点已经是**被害者**。
  这个作业用 Debug 构建（调试堆有守卫字节）跑测试，并在**每个用例之后**校验一次堆
  （`tests/main.cpp` 的 `HeapCheckListener`），把"第一个让堆变得不一致的用例"直接指出来；
  再逐个用例单独进程跑一遍作为兜底。新增 `ci-debug` 预设（Debug，只编 core 测试）。
- **Windows ASan 作业**（CI 的 `windows-asan`，同样不影响发布）—— 给上面那条诊断换一台
  **看得见元凶**的仪器。两者看得见的范围不一样：CRT 调试堆只在**每块分配的边界**放
  守卫字节，越界十几字节以上就落进相邻块、守卫字节依然完好（本机实测：故意踩坏块头，
  `HeapValidate` 也照样返回 TRUE）；ASan 给每块分配加红区，并在每次读写上插检查，
  越界与"使用已释放内存"都会**在发生的那一行**报出来，不管落点是不是别人的块。
  用 Release 配置（ASan 要求 `/MD`，与 Debug CRT 不兼容，而 `0xc0000374` 恰好只在
  Release 出现）。`BATCHSMITH_LTO=OFF` 也传了，但**别把它当成在 Windows 上跑通 ASan
  的条件**：CMakeLists 里的 LTO 分支写着 `if(BATCHSMITH_LTO AND NOT MSVC)`，
  MSVC 上这个开关本来就是空的。这个作业的**绿色只允许**
  表示"ASan 真的跑完了、什么都没报"；配置/构建/产物/测试没跑起来一律判红，但注解里
  写清"是本站作业坏了，不是被测代码有问题" —— 干净的运行另发一条正向注解。
- **Windows 内存证据包**（CI 的 `windows-memdump`）：把"能带出 CI 的证据"打包挂到
  一个预发布 `ci-diag` 上（每次覆盖）。动机是 ASan 干净之后**换一台不插桩的仪器**：
  **PageHeap**（靠 IFEO 的 `GlobalFlag`/`PageHeapFlags` 打开，不需要 `gflags.exe`，
  windows-latest 上也没有它）—— 保护页会让越界**在写的那条指令上**变成 AV，
  而不是等堆元数据被踩坏之后由 OS 事后发现。采集内容：PageHeap 跑的输出与崩溃前用例、
  **WER 事件日志（XML，含出错模块 + 偏移 + 异常码）**、链接器 **`.map`**（有了它，
  "模块 + 偏移"可以在**本地**翻成函数名 —— 不需要调试器，也不需要仓库写权限）、
  普通 Release 跑的输出、以及体积可控时的迷你转储。
  ⚠️ 这个作业带**自检**：先用一个故意越界 1 字节的探针程序确认全页堆真的开着 ——
  否则"PageHeap 下没崩"到底是没问题、还是仪器根本没开，分不清（本仓库踩过的
  "沉默不等于干净"）。作业本身 `continue-on-error`，采集失败不拖红整轮 CI。
- **`build` 作业的 Windows 测试步骤临时不阻断整轮 CI**（`continue-on-error`，
  只对**非 tag 推送**生效）。那个 `0xc0000374` 从引入 Lua 沙箱起就一直挂着，
  "每推一次整轮红"只会让人习惯性忽略红色；证据与结论改由 `windows-memdump` 报。
  这一步在编排界面里**照样标红**，且 **tag 推送照旧阻断**（免得坏着发版）。
  定位之后把那一行删掉。

- **诊断作业改用「序号」选用例，并给 bash 的退出码配一把「翻译尺」**（run #21 之后的修正）：
  - `windows-heapcheck` / `windows-memdump` 原先用 `-tc="$用例名"` 选单个用例逐个跑。
    **这在 Windows 上根本不成立**：用例名是中文，而非 ASCII 的 argv 会按 ANSI 代码页解码，
    跟二进制里的 UTF-8 字面量对不上 ⇒ 过滤命中 0 个 ⇒ 每次单跑都"成功"（跑 0 个、退出码 0）。
    本机实测（MinGW Release）：`-tc="区段只允许表达式：赋值语句编译不过"`
    → `test cases: 0 | 0 passed | 0 failed | 155 skipped`，而 `--first=56 --last=56`
    真的跑到了那个用例。也就是说 heapcheck 那句"155 个用例逐个单跑、0 个异常退出"
    一直是在**空转**，什么都没证明。现在改用**序号**（`--first`/`--last`，纯 ASCII），
    并加了自检：**只要有一次单跑选到 0 个用例，就判通道坏、作业判红**。
  - `windows-memdump` 额外把**嫌疑用例各自单独跑一个进程**（Release，且先把 PageHeap 关掉），
    再加一次 `r55-57` 连跑 —— 用来区分"自己崩"与"被前面的用例带崩"。序号取自
    `--list-test-cases` 的顺序，那三个用例正好是 55/56/57。
  - **bash 报的退出码是残缺的**，见下面「修复」里的 127 条目。摘要里现在并列
    bash 退出码与 PowerShell 的完整退出码。
- **上传证据包失败时自曝原因**：`windows-memdump` 的上传步原先只留下
  `Process completed with exit code 1`，看不出是权限、tag 冲突还是网络。
  现在把 `gh` 的 stderr 原文与 `gh auth status` 一起提升成注解，而且**不再判红** ——
  结论已由采集步那条注解带出，附件只是补充。
- **Windows 分配器审计**（CI 的 `windows-alloc-audit` ＋ 构建开关 `BATCHSMITH_ALLOC_AUDIT`，
  默认关）：给沙箱的 Lua 分配器加**逐块记账**，专门查"分配器收到的入参是否自洽"
  （free / realloc 收到了不属于它的指针，或 `old_size` 与当年申请的不符）。
  动机是一条**排除法**：越界写这类原因基本可以划掉 —— ASan 插桩与 Debug 的 CRT 调试堆
  都会在事发时当场报出来，而两者在同一套 155 个用例上全绿；剩下的高频嫌疑就是
  "脏入参"这一类，它在 ASan 的替换分配器下会被消化成正常调用，在 NT 堆的 LFH 上
  却直接判堆损坏、一句话都不说。违规时以**退出码 77** 停机并把是哪一种违规写到 stderr
  （选 77 是因为它能穿过 bash 的退出码翻译，127 不能）。作业照旧带自检：
  配置摘要里没有「分配器审计：ON」就判红 —— 仪器没开的话，"没抓到"这条结论一文不值。
- **内存证据包新增 ④c：用 CLI（`bs eval`）在 PageHeap 下复现同一条中止路径**。
  动机是 PageHeap 把这条 bug 照出来了、却**看不见出错位置**：全页堆下第 57 个用例
  不再堆损坏，而是当场 AV —— 但那一句是 **doctest** 打的
  （`FATAL ERROR: test case CRASHED: SIGSEGV`），异常被它的未处理异常过滤器接住，
  **进不了 WER**，于是拿不到"出错模块 + 偏移"，`.map` 也就无从翻译。
  而 `bs eval` 走的是同一个 core `evaluate_template` 却没有 doctest：同一模板在
  PageHeap 下踩到同一个地址时，AV 是**未处理异常**，WER 事件里会带 Fault offset，
  配 `bs.map` 就能翻成函数名。（模板里的 `$(…)` 必须单引号包住，否则 bash 会去执行它。）
- **诊断结论（run #26，`50f4f6a`）**：分配器审计这一条**未触发** —— 也就是
  free / realloc 收到的指针都属于本分配器、`old_size` 与记账一致，`lua_close` 之后
  也没有未释放的块。加上此前 ASan 与 Debug 调试堆的双双全绿，
  "分配器收到脏入参"这一类原因**可以划掉**。剩下的口径收窄成：
  **一处只在 MSVC Release + LFH 布局下才致命的写越界，落在 ASan 保护不到的那类内存上** ——
  最可能是 Qt（`Qt6Core.dll`，非插桩）分配出来的块：ASan 的 Windows 实现只保护**插桩模块**
  里的分配，而 PageHeap 是对所有堆生效的，这恰好解释了"ASan 全绿 / 全页堆当场 AV"这个组合。
  ⚠️ 顺带记下一条取数经验：**作业级 `continue-on-error` 会让 jobs 接口把失败的作业报成
  `success`**（`Windows x64` 在 run #26 就是这样 —— run 结论 success，日志里却是
  `Exit code 0xc0000374`）。所以判断"这个作业到底红没红"必须读日志或注解，
  **不能看 `jobs[].conclusion`**。
- **CLI 崩溃自报**（构建开关 `BATCHSMITH_CRASH_REPORT`，默认关；CI 的 `windows-memdump`
  用来做 ④c）：`bs` 在 `main()` **第一件事**就装一个 vectored exception handler，
  崩溃时打印**出错指令相对主模块基址的 RVA**、访问类型（读/写）与出错地址，
  然后以**退出码 42** 立刻退出。实现见 `src/cli/src/crash_report.cpp`，那里同时写了
  "为什么不再绕 WER"。
  配套一条**仪器自检**：`BATCHSMITH_CRASH_SELFTEST=1 bs --version` 会故意解引用空指针，
  必须打出那行自报且退出码为 42 —— "没打出结论"与"仪器没装上"必须能分开，
  这个项目已经在"仪器其实没生效"上白扔过好几轮（采集步静默死、审计开关没打开……）。
  诊断链路至此变成：**PageHeap 让它当场 AV → CLI 自报 RVA → `bs.map` 翻函数名**，
  全程不依赖 WER、不依赖转储、不依赖调试器。
- **实测更正（run #27）：`GitHub runner` 上 WER 是空的，此前"绕开 doctest 让 WER 记
  Fault offset"这条路走不通。** 证据包里 `wer-application-error.xml` 与 `wer-wer.xml`
  都是 **0 字节**、迷你转储一个都没生成（LocalDumps 依赖 WER，自然一起没有）；
  CLI 那次的输出文件同样 0 字节（`QTextStream` 有内部缓冲，进程被异常终结时没人替它 flush）。
  ⚠️ 但同一轮有一条**反证**：PageHeap 整跑打出
  `test cases: 57 | 56 passed | 1 failed | 98 skipped` —— 说明这个 AV
  **是能被进程内处理器拿到的**，只是拿不到的东西不在进程里。这就是上面那条改动的由来。
  同一轮还确认：PageHeap 下 `bs.exe` 跑同一条中止路径 **退出码 139（SIGSEGV）**
  ⇒ 这条 bug 在**没有 doctest** 的环境里也照样复现，④c 的前提成立。
- **实测更正（run #28，`39e321b`）：崩溃自报第一次跑通，就照出了它自己的盲区 ——
  真崩的出错指令**不在 `bs.exe` 里**。** 同一条中止路径在 PageHeap 下当场 AV：
  异常码 `0xc0000005`、**写**到 `0x1974625afc0`（一个垃圾地址）、出错指令落在
  **外部模块**（基址 `0x7ffe31e30000`）。而当时的自报只对**主模块**折算 RVA
  ⇒ 最有价值的那一条信息只打出一个 `0`。
  同一轮还纠正了一条旧口径：PageHeap 对 `bs.exe` 是**开着的**（IFEO 的
  `GlobalFlag=0x02000000`），bug 在全页堆下照样复现 ⇒「只在 LFH 布局下才致命」
  这个说法**不成立** —— 全页堆是页粒度 + 守卫页的另一套分配器，它做的恰恰是把
  "无声的堆破坏"变成"当场 AV"。
  于是自报升级成**模块名 + RVA + 调用链**：不再假定崩在主模块里，并且用 PE 的 unwind
  信息（`.pdata`）把调用链走一遍（等价于 `StackWalk64` 的核心，
  **不需要 PDB、不需要 dbghelp**）。配套把仪器自检加严：从"能打出 RVA 且退出码 42"
  改成"**还能走出 ≥ 3 帧调用链**"—— 只验前者等于没验这次真正要用的那条通路。
  采集侧同步：`symbolicate.py` 新增 `--frames`，逐帧翻符号。⚠️ **调用链里 #1 起都是
  返回地址（`call` 的下一条），必须按 `RVA - 1` 查**，否则会翻到相邻函数上去，
  而且看起来还挺像那么回事 —— 这类错误从输出上读不出来。
- ⭐ **诊断结论（run #30，`08da586`）：崩溃点定位到 `dsl::evaluate_template` 里一次对
  `Qt6Core.dll` 的调用。** 升级后的自报一次给全了调用链：
  ```
  异常码 0xc0000005  **写** 地址 0x19c046c6fc0（垃圾地址）
  所在模块 : Qt6Core.dll + 0x10860（**外部模块**）
    #0  Qt6Core.dll + 0x10860      ← 出错指令
    #1  bs.exe + 0x1d145  ⇒ dsl::evaluate_template（engine.cpp）
    #2  bs.exe + 0x6f65   ⇒ run_eval            （main.cpp）
    #3  bs.exe + 0x83fb   ⇒ main
  ```
  `#0` 的 RVA 与 run #28 **完全相同**（`0x10860`）⇒ 确定性行为，不是随机踩内存。
  对应源码位置是沙箱中止后的错误分支（`QStringLiteral("已中止：%1").arg(...)`）。
  ⚠️ 但**外部模块的 RVA 用 `bs.map` 翻不了**（那份 map 只覆盖 `bs.exe`）⇒ 证据包现在
  把 **`bs.exe` 与 `Qt6Core.dll` 本身**一起带回来，好查它们的**导出表**。
- **`pe_rva.py`：从 PE 里查「这个 RVA 落在哪个函数里」**（本机没有 MSVC 版 Qt，
  只能把 DLL 带回来自己查）。⚠️ 导出表里 `AddressOfFunctions` 按**序号**排、
  `AddressOfNames` 按**字典序**排，两者靠 `AddressOfNameOrdinals` 关联 ——
  照 `objdump -p` 的行号对齐就是张冠李戴（名字看着都挺合理，从输出上读不出来）。
  工具内置 **`.pdata` 交叉自证**：导出函数的 RVA 应当正好是某个函数起点，
  实测本机 Qt6Core.dll 为 **91.9%（5311/5778）**；若两张表错位，这个比例会塌。
- ⭐⭐ **诊断结论（run #31，`6dd9f9c`）：崩点是一处 Qt 的「引用计数自增」写。**
  把 `Qt6Core.dll + 0x10860` 用导出表一翻，它落在 `??0QString@@QEAA@AEBV0@@Z`
  （`QString` 拷贝构造）内部；反汇编是
  ```
  180010840: mov  (%rdx),%r8        ; r8 = 源对象的 d
  180010843: mov  %r8,(%rcx)
  180010846: mov  0x8(%rdx),%rax    ; 数据指针
  18001084e: mov  0x10(%rdx),%rax   ; 长度
  180010856: test %r8,%r8
  180010859: je   0x180010865
  18001085b: mov  $0x1,%eax
  180010860: lock xadd %eax,(%r8)   ← 崩在这里
  ```
  ⇒ **不是"堆管理器自己报错"，而是对源 `QString` 的 `d` 做原子自增时写到了坏地址。**
  再用 `bs.exe` 的**导入表**反查调用点：`#1` 那个 `call` 的目标正是
  `QString::QString(const QString&)`，而它的 `%rdx` = `rbp+0x170`，
  `%rbp+0x168` 恰好是 `box.m_violation`（紧跟其后的 4 字节比较）
  ⇒ **源对象就是 `Sandbox::m_violation_message`**，与 `sandbox.hpp` 的成员顺序、
  以及 `engine.cpp:162` 那句完全对上；那个静态字面量也已在 `bs.exe` 的 `.rdata`
  里核对过 = `已中止：%1`。
  ⇒ 问题收敛为：**`m_violation_message` 不是一个健康的 QString**，
  它的 `d` 三轮都落在"页内偏移恒为 `0xfc0`"的地址上
  （`0x1974625afc0` / `0x19c046c6fc0` / `0x1ce37a86fc0`）。
- **崩溃自报再补三样证据（`42eb0e3`）**：`m_violation_message` 的 `d` 是"已归还系统的堆、
  线程栈、还是从来没映射过"——光有裸地址分不出来，上一轮就卡在这里只能猜。
  - `被访问页`：把访问地址用 `VirtualQuery` 折成「状态/类型/保护/区域大小」。
    `空闲` = 那片内存已归还系统（use-after-free 的铁证）；
    `已提交/私有` = 内存确实存在、只是不属于这个对象（对象被覆盖或从未初始化）。
  - `寄存器`：`rip/rsp/rbp/rcx/rdx/r8/r9` 全打（x64 头两个整型参数位就是 `rcx/rdx`）。
  - `rcx/rdx 指向`：把那 24 字节按 **Qt 容器三件套（数据头指针/数据指针/长度）**打出来，
    并对第一个 qword 单独查一次区域性质 ⇒ 能分清"整块都是垃圾"与"只有 `d` 被写坏"。
    ⚠️ 读取前一律先用 `VirtualQuery` 确认可读：野指针本来就可能落在未映射页上，
    在 VEH 里去读它等于再崩一次、现场就没了。
  - 自检判据同步加一条（缺了判红）：报告里必须有**寄存器行**与**嫌疑对象行** ——
    免得仪器"看起来完整"而其实少了这一块。
- **`iat.py`：把 `call *0x...(%rip)` 命中的 IAT 槽翻回「哪个 DLL 的哪个导入函数」**
  （这轮定位调用点就靠它）。⚠️ `objdump -p` 里那列 `vma:` 是 **`IMAGE_IMPORT_BY_NAME`
  的 RVA**，不是 IAT 槽地址 —— 照数字对齐会张冠李戴；正确做法是走
  `IMAGE_DIRECTORY_ENTRY_IMPORT`：`槽地址 = FirstThunk + i*8`，名字取
  `OriginalFirstThunk[i] + 2`。⚠️ 另一处同源坑：thunk 里存的是 **RVA**，
  必须过一次 `RVA → 文件偏移` 换算，否则读出来的是垃圾（表现是"导入名全是 `?`"，
  看起来只像名字没解析出来）。
  工具内置 `--selftest`（每条导入反查自身），实测 MSVC `bs.exe` 286 条 / 0 不一致、
  本机 MinGW `bs.exe` 282 条 / 0 不一致。
- ⭐⭐⭐ **诊断结论（run #32，`42eb0e3`）：不是"对象被踩坏"，而是"它指的内存已被释放"。**
  新加的三样证据第一次把"那个 `d` 到底是什么"说清了：
  ```
  被访问页 : 已保留/私有 区域 8K
  寄存器   : rsp=0x56d4afe208 rbp=0x56d4afe310 rcx=0x56d4afe2f8 rdx=0x56d4afe480 r8=0x24e37156fc0
  rdx 指向 : 0x56d4afe480
     第1 qword (d)    = 0x24e37156fc0 → 已保留/私有 (NOT writable)
     第2 qword (ptr)  = 0x24e37156fd0  = d+0x10  ✓
     第3 qword (size) = 17  (= 「指令数超过上限（20000000）」的长度) ✓✓✓
  ```
  三件套**完全自洽**（`ptr == d + 16`、`size` 正好是消息长度），可 `d` 所在的页是
  **`MEM_RESERVE`（已保留但未提交）** —— 页没提交，写上去必然 AV。
  ⇒ 结论从"垃圾指针/被踩坏"改成 **`QString` 的数据块被提前释放**（use-after-free），
  而且**排除了**"未初始化"和"缓冲区溢出写坏了对象头"这两个方向。
  ⚠️ 顺带否掉一条诱惑：别拿 `qrefcount.h` 里 `RefCount::ref()` 的 `count != -1` 检查
  去推演——**实际导出的那份 Qt6Core 拷贝构造里没有这个判断**（反汇编已核对），
  照着头文件推理会得出与实际相反的结论。
- **消息追踪探针（本轮新增，为把"什么时候丢的"二分出来）**：
  `Sandbox::trace_violation_message(where)` 在**注入列表之后 / raise 赋值之后 / 拷贝之前**
  三处各看一眼 `m_violation_message` 的**数据块与头部所在页的性质**，
  加上"数据指针有没有换过"（`指针首见/未变/**变了**`）——于是"在哪一步坏的"一次就能定位，
  不必再一轮一个猜想。
  - ⚠️ **两个开关是分开的，缺一不可**：编译期 `BATCHSMITH_MESSAGE_TRACE` 决定
    *实现存不存在*，运行期环境变量 `BATCHSMITH_MESSAGE_TRACE=1` 决定*打不打印*。
    分开是因为同一次诊断构建里的单元测试也会跑到中止路径，默认打开会把测试输出刷爆。
    而关着时的表现特别阴：**实现是空函数，日志看起来完全正常** ⇒
    "探针没说话"极易被读成"消息一直是好的"。所以 CI 里两道自证都加了：
    配置摘要必须有「消息追踪」那一行，跑完必须数出探针行数（0 行直接判红）。
  - ⚠️ 探针内部**绝对不能**写 `const QString copy = m_violation_message;` ——
    那正是我们要观察的那次引用计数自增，消息真悬垂时探针会**抢在真正的位置之前崩掉**，
    于是"崩在探针里"看起来像"探针坏了"，最该看的信息反而丢了。
    实现里只读 `constData()` 与 `size()`，全程不碰引用计数。
  - 只在 Windows 上有实现（要 `VirtualQuery`），非 Windows 或关掉开关时是空调用，零开销。
- ⭐⭐⭐⭐ **诊断结论（run #33，`761440f`）：区间缩到「`raise()` 里是好的、返回之后就是坏的」，
  而那三行探针的读数又自相矛盾 —— 这一步先把自己的仪器查清楚。**
  探针第一次跑出来的三行是：
  ```
  注入列表之后（还没跑任何 Lua）：数据 …CCC140（已提交/映像/只读）        长度=0   指针首见
  raise 赋值之后：              数据 191E8BDFFD0（已提交/私有/读写 4K） 长度=17  指针**变了**
  中止分支：拷贝之前：          数据 191E8BDFFD0（已保留/私有 8K）      长度=17  指针未变
  ```
  前半段结论很硬：**`raise()` 赋值那一刻消息是健康可写的**（新分配、读写页），
  出了 `raise()` 就只剩「已保留」⇒ 释放发生在 `raise()` 返回那一段，
  而不是"构造消息时就坏了"。
  ⚠️ 但第三行**同时**报「已保留」又读出「长度=17」—— 而崩溃自报的 VEH 会拦下**任何**
  访问违例（含读），真读不动的话我们早该看到一次自报。**两者不可能同时为真。**
- **先把地基打实：本机实测「已保留」到底能不能读**（`.workbuddy/tools/pageprobe/decommit_read.c`）。
  做法是造出 7 种页状态，每种都**各起一个进程**做读 / 写，用退出码当判据
  （⚠️ 本机 MinGW 的 g++ 不支持 `__try/__except`，编译直接报 not declared ⇒
  不能靠 SEH 兜，只能靠"一个访问一个进程"把它变成可观察的判据）：
  | 页状态 | 读 | 写 |
  |---|---|---|
  | 已提交 | 成功 | 成功 |
  | **已保留**（`MEM_DECOMMIT`） | **AV** | **AV** |
  | 空闲（`MEM_RELEASE`） | AV | AV |
  | `PAGE_READONLY` | 成功 | AV |
  ⇒ **「已保留」= 读写都当场 AV**，所以那句「长度=17」不可能真是读出来的。
- **探针据此重做（本轮的第二刀）**，三处修正都是冲着"读数不可信"去的：
  - **先判可读、再读**（`page_readable()`，与崩溃自报同款判据）：读不动就明说「读不动」，
    绝不越过这一步 —— 探针自己踩上去会把现场弄丢，还会把崩点变成探针的地址。
  - **长度改从头部字节取**（`ref` / `size` / `flags` 三个字段 `memcpy` 出来），
    不再经 `QString::size()` —— 只要长度是"经 QString 读出来的"，就永远说不清
    它到底有没有真读内存。
  - **加 `HeapSize` 交叉验证**：遍历 `GetProcessHeaps()`，看这块地址还算不算某个堆里
    **已分配**的块。它和 `VirtualQuery` 是**两条独立的证据链** —— 前两轮的结论
    （"已保留 ⇒ 内存已被释放"）一直只有 `VirtualQuery` 一个证据源；
    两边都指向「已释放」才敢下结论，若二者打架，是我们对其中一个的理解错了。
- **探针点从 3 个增到 6 个**，把「释放发生在临时析构」与「发生在 Lua 错误传播」切开：
  `注入列表之后` → `raise 赋值之前(参数)` → `raise 赋值之后(参数/成员)`
  → **`limit_hook: raise 返回之后(成员)`** → `中止分支: 拷贝之前(成员)`。
  中间那个分界点是关键：`raise()` 返回时，`.arg()` 的临时对象**已经析构**
  （它绑在 `const QString &` 形参上，生命周期到那条完整表达式结束），
  而 Lua 的错误传播还没开始 —— 到这里就坏，说明是临时析构时被多减了一次引用计数。
  同时新打出的 `ref` 给出**直接判据**：赋值前参数应为 `ref=1`、赋值后参数与成员共享
  同一块且 `ref=2`；**若赋值后仍是 `ref=1`，就是"这次赋值没自增"** ——
  那么调用方的临时对象析构会把它减到 0 并释放，后面那次拷贝自然 AV。
- ⚠️⚠️ **run #34 的 `0xc0000374` 是「取证工具自己制造了要取证的故障」—— 那一轮结论作废，
  但教训很值：** 该轮只打出一行探针就崩，异常码 `0xc0000374`（堆损坏）、崩在 ntdll，
  调用链一路翻符号之后是
  ```
  #6 trace_message +0x35f     ← 崩在这里
  #7 Sandbox::raise → #8 limit_hook → luaD_hook → luaV_execute → evaluate_template
  前 6 帧全是 ntdll.dll 的堆内部
  ```
  再用本机工具反汇编 `trace_message+0x35f`：
  ```
  1400430ea: call *0x37f58(%rip)   # 0x14007b048
  ```
  拿 `iat.py` 把那个 IAT 槽翻出来 = **`KERNEL32.dll!HeapSize`**，
  参数正是我传进去的 `QString` **数据指针**（`d + 16`，**块内部的地址**）。
  ⇒ 那条"独立取证"的问题有两层：
  1. **地址形态错了**：给堆 API 的必须是**块首**（对 `QString` 就是 `d` = `data - 16`，
     那才是 `malloc` 出来的地址）。看着像小事，实际是这一轮崩掉的原因。
  2. **`HeapSize` 不是只读探针，它会抛**：收到不属于它的指针时**不返回 `(SIZE_T)-1`，
     而是直接抛 `STATUS_HEAP_CORRUPTION`**。所以它不能用作"无副作用取证"。
  ⇒ 这个假故障一度看起来像"终于把 CI 的堆损坏复现出来了"，**差点把整轮方向带偏**。
  已把它彻底删掉，并在 CI 里加了负向守卫（`GetProcessHeaps(` 不许再出现）。
  一般化的教训：**诊断仪器本身也要先被验收** —— 这个项目已经在"仪器没生效"上白扔过几轮，
  这一轮是它的反面：仪器不但要"生效"，还得**不会自己制造故障**。
  留下的只有 `VirtualQuery` 一条，它的语义已用 7 种页状态 × 读写实测钉死。
- ⭐⭐⭐⭐⭐ **诊断结论（run #35，`2b20ea3`）："引用计数算错了"这个假设被否决。**
  探针（已改成先判可读再读 + 从头部字节取长度）这一次给出了**六行完全自洽**的读数：
  | 时点 | 看的是谁 | 数据指针 | 页性质 | ref / size / flags |
  |---|---|---|---|---|
  | 注入列表之后 | 成员 | `…C140` | 已提交/映像/只读 | 空串（无头可读） |
  | raise 赋值之前 | **参数** | `…3FD0` | 已提交/私有/读写 | **ref=1** size=0 flags=0x11 |
  | raise 赋值之后 | **参数** | `…3FD0` | 已提交/私有/读写 | **ref=2** |
  | raise 赋值之后 | 成员 | `…3FD0` | 已提交/私有/读写 | **ref=2**（同指针 ⇒ 确实共享） |
  | limit_hook：raise 返回之后 | 成员 | `…3FD0` | 已提交/私有/读写 | **ref=1** |
  | 中止分支：拷贝之前 | 成员 | `…3FD0` | **已保留/私有 8K** | **读不动（页不可读）** |
  ⇒ 三点结论，把之前几轮的猜测全部收敛：
  1. **`raise()` 的赋值是对的**（1→2，参数与成员共享同一块）；
  2. **临时对象析构也是对的**（2→1，且此时页仍是"读写"）⇒
     **"引用计数少算了一次"这条假设被实测否决**，不用再往那个方向找；
  3. 同指针、且进入中止分支前就"读不动" ⇒ **块是在 `raise()` 返回之后、
     中止分支之前被释放的**，也就是 **Lua 的错误传播那一段**
     （`luaL_error` 长跳 → `luaD_pcall` → 回到引擎）。
  ⚠️ 附带说明：`ref` 一列可以直接当"这次赋值有没有自增"的判据，
  而且它同时解释了为什么前一版探针会在同一行里既报"已保留"又报出长度 ——
  **那是读数不可信，不是代码有两个矛盾**（新版探针不再做不做判定的读）。
- **仪器自证：本机把探针在"健康路径"上先跑一遍**（`BATCHSMITH_MESSAGE_TRACE=1` + MinGW 的
  `bs.exe`，同一条中止命令）。这是本轮很值的一步 —— 它给出了上面那张表的**对照列**：
  MinGW 上除第一行（空串，落在映像里）之外，**其余六行的页性质一路都是"已提交/私有/读写"**
  （块健康活到中止分支，看门狗零命中），`已中止：指令数超过上限（20000000）` 正常输出；
  不设环境变量时**输出完全安静**（空实现那条路径也验了）。
  ⇒ 于是"MSVC 上走到第 6 行之后页变已保留"是一个**真实的平台差异**，不是探针的错觉。
  ⚠️ 顺带被这次自检抓出一个小毛病：**空 `QString` 的 `data` 不能按 `data - 16` 取头**
  （它指向 Qt 的静态空数据，`offset` 与普通堆块不同；本机读出来是 `ref=1313431379` 这种垃圾）。
  现在空串直接标「（空串：无数据头可读）」，并且**不再给它挂看门狗**。
- **Lua 分配器看门狗（本轮新增，用来直接回答"谁释放的"）**：
  探针一旦看到真实的非空消息块，就把它的**块首**记下来；此后沙箱自己的
  `tracked_allocator()` 每次被调用（无论分配、重分配还是释放）都与
  「块首 ± 64 字节」比一次，命中就打一行 `⚠️⚠️ Lua 分配器动了被盯的块：释放 ptr=… 偏移=16`。
  - 它**不依赖任何堆/页 API**（上一轮的 `HeapSize` 就是栽在"依赖堆 API、而且会抛"上），
    而且比事后反推省事得多：谁碰了这块内存，是它自己喊出来的。
  - 判据：出现 `释放` + 偏移 16（正好是 `QString` 数据指针相对块首的偏移）⇒
    就是 Lua 分配器释放的；一条都没有 ⇒ 释放者不是它，得换方向（但先确认看门狗真挂上了
    —— 它在「raise 赋值之前 / 参数」那行之后就挂好了，早于它的命中不会报）。
  - CI 侧同步：新增看门狗命中行数的统计与自证（`看门狗命中: N 行`），探针期望行数 6 → 7。
- ⭐⭐⭐⭐⭐ **诊断结论（run #36，`57f06a2`）：Lua 分配器也不是凶手 —— 那一减就在 `lua_pcall` 里。**
  看门狗（沙箱自己的 Lua 分配器每次调用都与「消息块首 ±64 字节」比一次）**0 命中** ⇒
  不是它干的；而区间又被切了一刀：

  | 时点 | ref | 页性质 |
  |---|---|---|
  | limit_hook：raise 返回之后 | 1 | 已提交/私有/读写 |
  | lua_pcall 返回之后 | — | **已保留/私有（读不动）** |

  ⇒ 释放发生在 `raise()` 返回之后、`lua_pcall` 返回之前，也就是**纯 Lua 错误传播那一段**。
- ⭐⭐⭐⭐⭐⭐ **诊断结论（run #37，`d7a4eab`）：块是被「引用计数多减一次」释放的
  —— AV 与 HEAP_CORRUPTION 两个症状至此归到同一条因上。**
  手段：在 `raise()` 里**额外多持一份引用**（进程内只留一份），把两种机制一刀切开。
  **八行探针**（新增「中止分支：拷贝之后」）：

  | 时点 | ref | 页性质 |
  |---|---|---|
  | raise 赋值之前（参数） | 1 | 已提交/私有/读写 |
  | raise 赋值之后（参数 / 成员） | 2 | 同上 |
  | limit_hook：raise 返回之后 | **2**（多持的那份在位） | 同上 |
  | lua_pcall 返回之后 | **1** | 同上 ← 掉了一次，**但块还活着** |
  | 中止分支：拷贝之前 / 之后 | 1 | 同上（**没崩**） |

  ⇒ 三条收敛的结论：
  1. **多持一份引用确实把块救回来了**（中止分支那次拷贝平安走完）⇒ 释放走的是
     **引用计数这条路**，不是「绕过引用计数直接 free」；
  2. 探针 5→6 之间**净掉了一次**（2→1），而 `m_violation_message` 仍然指着那一块
     ⇒ **有人偷走了它那份引用**（一次多余的 deref）；`raise()` 的赋值与临时析构都正常；
  3. 块最终仍被释放，崩点顺势挪到**退出时那个 `QString` 的析构** ——
     `Qt6Core.dll + 0x418d`（`??1QString@@QEAA@XZ` + 0xd，正是引用计数那一写），
     调用链 `__scrt_common_main_seh + 0x176` ⇒ 在 `exit()` 里跑静态析构、**没有 main 帧**；
     崩时那个对象是 `bs.exe` 数据段里的一个 `QString`（也就是多持的那一份），
     而页已是「已保留/私有 区域 **48K**」（4K → 48K：释放后与邻居合并了）。

  ⭐ **两个症状至此归到同一条因**：引用计数被多减一次 ⇒ 减到 0 就提前释放 ⇒ PageHeap 下
  页被 decommit，**下一次访问 AV**（`0xc0000005`，正是历史崩点：中止分支那次拷贝）；
  而**普通堆**下块只是回到自由链，**同一块被释放第二次** ⇒ 堆校验当场报
  **`0xc0000374`**（正是 CI 单元测试长期那个红点）。所以它不是「堆元数据被踩坏」，
  **是一处引用计数多减**。
  ⚠️ **本机（MinGW）同一条路径上没有那一减**（探针 5→6 的 ref 一直是 2）⇒ 这是
  MSVC + 官方 Qt 上才出现的差异，只能在 CI 上抓。
- **写看门狗（run #38 新增）：让那次多余的写自己撞墙。**
  那一减发生在纯 Lua 长跳窗口里（那一段**没有一行 Qt 代码**），探针已经切不动了，
  于是换一类仪器：把消息块**所在整页改成只读**（`VirtualProtect(PAGE_READONLY)`），
  任何写它的指令当场 AV，VEH 里报出**模块 + RVA**（`describe_owner` 用 `AllocationBase`
  折算，与崩溃自报同口径）；报完恢复可写、再用**单步（TF）**重新挂上，最多报 6 次
  ⇒ 日志里能按时间顺序看到「一串写」，每行还带「该地址原值=N」（块头 +0 就是引用计数）。
  - ⚠️ 命中意味着那次写**不生效**（AV 发生在指令完成之前）⇒ 流程会「变得正常」，这是预期的：
    仪器只负责指认凶手，不负责让现场继续崩。
  - **挂在 `limit_hook` 里、而不是 `raise()` 里**：`raise(...)` 那条语句收尾时参数临时对象
    会析构，那也是一次合法的引用计数写 —— 挂早了第一个命中的就是它。
  - 中止分支拷贝走完就**摘掉**（`disarm_message_write_watch`）：再往后块随时可能被堆释放，
    让堆去写一个只读页就是仪器自己制造故障了。
  - **仪器自检**：`BATCHSMITH_MESSAGE_PAGE_WATCH_SELFTEST=1` 时先在一块自造内存上故意写一下，
    必须看到命中行；**自检段没命中 ⇒ 作业判红**（否则「0 命中」会被读成「没人写」，
    而它同样可能只是「页根本没改成只读」—— 这正是 `HeapSize` 那一轮栽过的坑）。
  - 本机（MinGW）已验：自检命中行落在 `bs.exe`（就是自检里那句故意写），
    之后真目标上的两次命中分别是拷贝构造的自增（原值=2）与临时析构（原值=3），
    位置与语义都对得上 —— 仪器可用；同时它给出的对照也再次确认**本机没有那一减**。
- **诊断结论（run #39，`6379bcb`）：同一份代码再跑一轮，**依然干净** —— 于是先别急着
  说"没复现"，得先回答一个更基本的问题。**
  run #38 与 run #39 是**逐字节相同的二进制**（后者是空提交），两轮都是 155/155、
  中止路径退出码 1、探针 5→6 的 ref 一直是 2；而 run #36 / #37（**没有**写看门狗）都拿到了
  那一减。1 比 2 的样本量分不开两种解释：

  1. **运行时效应** —— 把那一页改成只读这件事本身把那一减挡住了；
  2. **编译期效应** —— 新加的那几行代码改了代码/堆布局，bug 被遮住了。
- **A/B 复现率（run #40 新增）：同一份二进制，只差环境变量。**
  `BATCHSMITH_MESSAGE_PAGE_WATCH` 是**运行期**开关（`page_watch_on()`）：关着的时候
  `arm_message_write_watch()` 立刻返回 —— 不 `VirtualProtect`、不跑自检、不装页看门狗 VEH
  ⇒ **A 组在运行时与 run #37 那份二进制等价**。于是各跑 N 次（默认 20），逐次记一行读数
  （`ref(raise)` / `ref(lua_pcall)` / 退出码 / 是否当场 AV），并把汇总走**注解**（job 日志
  匿名读不到）。三分法：
  - A 脏而 B 干净 ⇒ ①，那页只读才是遮住它的东西 ⇒ 换**不碰页保护**的仪器：
    x64 硬件写断点（DR0 盯块头 +0 那 4 字节），只改调试寄存器、不动堆与页；
  - 两组都脏 ⇒ ② 不成立，且 B 组的命中行会**直接点名**那一减的模块 + RVA；
  - 两组都干净 ⇒ ②（布局），回到"仪器越少越好"的思路重做。
  - **分组必须自证**：只数「已挂 ≥ 1」是不够的 —— **自检那一次也会打一条「已挂」**，
    于是"真目标根本没挂上"照样能过。判据改成 **A 组 0 条、B 组 ≥ 2 条（自检 + 真目标）**。
  - 本机预演（`.workbuddy/tools/ci-patch/ab-local.sh` + 本地 `-DBATCHSMITH_MESSAGE_TRACE=ON`
    构建）就撞上了这个坑：**B 组的"真目标"那一次挂页后当场 SIGSEGV**（本机堆不给块独占页，
    那块落在 **64K** 共享区里，把整页改成只读会连带打断邻居的写）⇒ 收紧后的判据把它报了出来。
    CI 上是 `区域 4K`（PageHeap 给块独占页）⇒ 确实挂得住，run #38 / #39 都拿到了真目标命中。
  - ⚠️ 遗留风险（下一轮修）：页看门狗的 VEH 没有**重入保护** —— 若打断的写发生在
    handler 自己要用到的同一页（本机 64K 共享页正是这种情形），报告本身会再触发异常。
    换 DR0 之后这个问题自然消失（那台仪器根本不动页保护）。
- ⭐⭐⭐⭐⭐ **重要更正（run #39 / #40 复查）：红点**一直没走**，A/B 也把「运行时效应」排除了。**
  两点新事实（都是这轮才拿到的）：

  1. **`windows-x64` 那份构建至今每一轮都红**：`batchsmith.unit` → `Exit code 0xc0000374`。
     它没被看见，是因为这一步带着 `continue-on-error`（非 tag 推送不阻断），
     作业结论被报成 `success` —— **判据只能读日志或注解**：
     注解里那条 `测试失败摘要：1/2 Test #1: batchsmith.unit …… Exit code 0xc0000374`
     才是真相（run #37 / #39 都是）。
  2. **崩点已由注解钉死**（`崩溃前最后跑到的用例`）：`tests/core/test_engine.cpp:178`
     的用例 **「区段只允许表达式：赋值语句编译不过」**，而且死在
     `test_engine.cpp(181): SUCCESS: CHECK_FALSE( result.ok() ) is correct!` **之后** ——
     即该用例**作用域结束时** `EvalResult::error` 那个 `QString` 的析构
     （`0xc0000374` = 同一块被释放两次）。
     ⇒ **修正方向**：这条路径上**根本没有沙箱的 violation message** —— `$x = 1$`
     能过扫描器（区段非空、`$` 配对），错误来自生成的 Lua `local s1 = (x = 1)` 在
     `luaL_loadbuffer` 上语法失败，经 `QString::fromUtf8(lua_tostring(...))` →
     `describe_error(compiled, raw)` 变成 `result.error`。前几轮把力气全押在
     `m_violation_message` 上，**崩的其实是另一个 QString（`EvalResult::error`）**。
  3. **run #40 的 A/B 结论**：A 组 20/20、B 组 20/20，逐次都是
     `ref(raise)=2 ref(lua_pcall)=2 退出码=1` —— **一次「少一减」都没有**
     （分组自证通过：A 组 0 条「已挂」、B 组 2 条）。⇒ ④c 那条中止路径上的那一减，
     在**当前这份（带探针的）二进制**里根本不发生 ⇒ 是**编译期效应**，不是"那页只读挡住了"。
  4. 把 1 与 3 合起来：diag 那份构建（`MESSAGE_TRACE=ON CRASH_REPORT=ON GUI=OFF`）
     在**普通堆**下跑同一批用例是 `155 passed | 0 failed`，而 `--preset ci` 那份稳定红，
     两边只差这几个开关 ⇒ **那份诊断构建把红点遮住了**（这也解释了为什么仪器越加越"好"）。
     ⚠️ `BATCHSMITH_STRICT` 在 MSVC 上只打一行提示、`LTO` 被 `AND NOT MSVC` 排除、
     `CRASH_REPORT` 的实现只编进 CLI（测试只链 core）⇒ 后两条正好是**阴性对照**。
- **诊断矩阵（run #41 新增）：在红点复现的那个作业里逐开关对照。**
  只改 `ci.yml` ⇒ 基线那份二进制**逐字节不变**，结论不会被自己的改动污染。四个变体各跑 5 次：
  `V0` 原样 `--preset ci`、`V1` `+MESSAGE_TRACE=ON`、`V2` `+CRASH_REPORT=ON`、`V3` `+BUILD_GUI=OFF`。
  - 判据用**日志内容**不用退出码：bash 对无法映射的 NTSTATUS 一律报 127（和"命令找不到"同码），
    所以只看"这一跑有没有打完"——正常跑完一定会打 `test cases: 155`，崩了就一定没有；
  - 自证：每个变体的 exe 必须存在且非空（否则"跑完 0 次"会被读成"红"）；
  - 汇总走**注解**（job 日志匿名读不到）。
  - 读法：`V1` 若 =5 而 `V0/V2/V3` 都 <5 ⇒ 就是那几行探针代码遮住了红点（编译期效应），
    **后续仪器必须搬出 core**（放进测试可执行文件或独立进程），否则永远看不到它；
    `V2/V3` 若也 =5 ⇒ 阴性对照失效，"这台机器上每次构建的堆布局本来就在掷骰子"，
    结论要改成「红点是随机的」再重做。
  - 本机预演：`.workbuddy/tools/ci-patch/dry-matrix.py`（把这一步的 bash 原样抽出来，
    只替换预设名与基线 exe，逐字照跑）—— 验的是**脚本本身**（能不能抽出 exe、
    能不能解析 `test cases:`、汇总变量有没有丢），不是"本机有没有那个崩溃"（本机没有）。
    ⚠️ 写这一步时先排掉了一处会静默自伤的东西：它原本想用 `mark()`，而 `mark()` 只在
    「内存证据包」那个作业里定义过 ⇒ 会打一串 `mark: command not found` 而**不报错**
    （本作业这一段没有 `set -e`），日志看起来照样"跑过了"。已改成这一步自带的 `step_mark()`。

### 修复

- **`windows-memdump` 的采集步其实从 run #23 起就在"跑了一半就死"**：`exe` 与 `map` 是
  用 `$PWD` 拼的，也就是 Git Bash 形态的 `/d/a/...`。bash 自己的程序（`ls` / `timeout` /
  `cp`）两种形态都认，所以前半段正常；但那个路径要交给 **python** 去 `subprocess.run`
  —— python 只认 `D:\a\...`。于是 `runexe.py` 一进去就 `FileNotFoundError`，
  而 `set -e` 把整个采集步**静默**中止：日志里只剩一句 `Process completed with exit code 1`，
  后面的 PageHeap 自检、PageHeap 整跑、嫌犯用例单跑、WER、转储、摘要**一个都没做**。
  判据是日志的时间差 —— `普通跑（bash）：退出码 127` 与那句 error 只隔 **0.15 秒**，
  而整跑本身要 ~1 秒，说明 python 根本没跑起来。现在交给 python 的路径一律过
  `cygpath -w`，并且**两侧都自证**：bash 侧先 `os.path.isfile` 验一次，
  `runexe.py` 自己也验一次（写清楚"是这站作业传错了路径"，而不是抛一个裸异常）。
  同类前科：`packaging` 里 `cygpath -m` 对相对入参原样返回那个坑 —— 都是"路径形态"问题。
- **采集步的"静默死"改成"指名道姓"**：这种一步到底的长脚本，任何一处未预期的非零退出
  都会把剩下的证据一起带走，而日志里只留一句 `exit code 1`（这一轮就是靠**时间差**才
  推出来死在哪儿）。现在每一段进入前打一行 `--- 采集进度: ② … ---`，
  日志里最后一条就是断点；`runexe` 也改成失败时**打一行可见输出**而不是让 `set -e`
  把整步带走。
- **④c 探针把证据包作业自己弄红了一整轮（run #26）：配置里关着 CLI，却让构建步去编 `bs` 目标**
  —— `windows-memdump` 的 diag 配置原本是 `-DBATCHSMITH_BUILD_CLI=OFF`，而 CLI 目标由
  `src/cli` 提供、只在 `BATCHSMITH_BUILD_CLI` 下 `add_subdirectory`（顶层 `CMakeLists.txt:149`）。
  于是构建步第一句就死在 `ninja: error: unknown target 'bs', did you mean '.'?`，
  自检/整跑/单跑/WER/转储/摘要**又**一个都没做。改法两处：
  ① 该配置改成 `BATCHSMITH_BUILD_CLI=ON`（只多编一个 CLI 目标，不改变 core/测试的编译参数，
  与"红点同配置"这一点无关）；
  ② 加一道**预检** —— 配置摘要在配置完成那一刻就已经打印了 `GUI=… CLI=… TESTS=…`，
  所以完全可以在动用构建时间**之前**就断言 `CLI=ON` 并判红。教训一般化成：
  **这个作业后面每一步要用到的目标/文件，都要在配置步之后先做一次存在性自证。**
  对应的"交叉一致性"检查也进了 `.workbuddy/tools/ci-patch/verify.py`：
  单看每一处都写对了、合起来却自相矛盾的那类改动，现在会被本机自检挡下。
- **转储汇总把"一个转储都没生成"说成"超出 25MB 上限"**：`total -eq 0` 与 `total > 上限`
  原先共用一句 `else`，给出的是一句**假因果** —— 会把人往"体积"上引，而真因是
  WER 的 LocalDumps 没生效或进程压根没崩在托管路径上（run #27 实测：确实是后者，
  runner 上 WER 根本不产事件）。现在三种情形分开说，0 字节那句直接点名要去查什么。
- **不再假定诊断产物的落点**：`/MAP` 产出的 `bs.map` 原先按 `build/diag/bin/bs.map` 硬取，
  而 MSVC 的 map 文件名虽取自输出文件、**目录并不保证与 exe 同处**。
  现在一律 `find build/diag -name 'bs.map' -print -quit` 去找，找不到就明说自己没找到 ——
  这类"假定某产物一定在某处"的写法已经连累过两轮（另一处是 `ninja: unknown target 'bs'`）。
- **Git Bash 的 `$?` 有歧义：127 既表示"命令找不到"，也表示"未映射的 NTSTATUS"**。
  本机用"抛出指定异常码的探针"（`.workbuddy/tools/statusprobe/exc.c`）实测：
  `0xC0000374`（堆损坏）→ **127**、`0xC00000FD`（栈溢出）→ **127**、
  `0xC0000005`（访问违例）→ 139（SIGSEGV）、`0xC000001D` → 132（SIGILL）。
  所以"退出码 127"根本不能拿来推断"脚本把 exe 路径写错了" —— run #21 就这么误判过一轮。
  要拿真正的异常码必须走保留完整 32 位值的途径（PowerShell 的 `Process.ExitCode`，
  或 ctest 自己的 `Exit code 0x…`）。诊断作业的摘要里现在带一张对照表。
- **YAML 块标量里的续行不能跑到列 0**：修 `ci.yml` 时写了
  `case_report="$case_report` ＋ 换行 ＋ 续行，续行掉到列 0 就把 `run: |` 这个块
  **提前结束了**，报错是一句离根因很远的 `could not find expected ':'`。
  已改成数组累加；改 ci.yml 的脚本（`.workbuddy/tools/ci-patch/`）也加了自检 ——
  块内每个非空行都必须带 YAML 块缩进。
- **`windows-heapcheck` 的自检误报了一整轮（是我自己加的检查写错了）**：自检原本看
  `test cases: 1 |` 这个数字，但 doctest 打的是「**通过过滤器的**用例数」，而
  `--first/--last` 的区间跳过是在这个计数**之后**才判的（`doctest.h` 的 run 循环）——
  单跑一个用例时它照样打 `test cases: 155 | 155 passed | 0 failed | 0 skipped`。
  于是 155 次单跑全被判成"空跑"、作业白红一轮。改用**断言数**作判据
  （真跑到用例就一定产生断言）。⚠️ 顺带修正一个更早的解读：PageHeap 那行
  `57 | 56 passed | 1 failed | 98 skipped` 里的 57 仍然是"循环走到的位置"
  （没传 `--first/--last` 时两者相等），那部分结论不变。
- **PowerShell 的 `Start-Process -PassThru` 拿不到退出码**：`ExitCode` 实测为 **null**
  （打印出来是 `exit=4294967296 hex=0x`），而且 stdout 重定向会落成 **0 字节文件**；
  空附件又被上传接口拒掉（`HTTP 400: Bad Content-Length`），连带整条
  `gh release upload a b c` 全失败。改用 **python**（`subprocess.run().returncode`，
  本机用抛 `0xC0000374` 的探针验证过：`returncode=3221226356`）；上传改为**逐个文件**上传
  并跳过 0 字节文件，个别失败只记数、不再拖红。
- **CI 里的"制品"取不出来：`actions/upload-artifact` 的下载接口匿名读不到**。
  实测 `GET /repos/<o>/<r>/actions/artifacts/<id>/zip` 匿名返回
  `401 {"message":"Requires authentication"}`，网页那条 `.../artifacts/<id>` 是 404。
  而**Release 附件可以匿名下载**（实测 Release 资产匿名 200）。所以诊断产物一律走
  Release 附件，不上 artifact —— 这不是"顺手换个上传方式"，而是"上传了也拿不到"。
- **`packaging/package-windows.sh` 传相对输出目录时会把归档打进 `dist/dist/`**：
  脚本先把两个目录过一遍 `cygpath -m`，再 `cd "$OUT_DIR"` 用 `$archive` 压缩 ——
  而 `cygpath -m` 对**相对入参是原样返回**的（实测 `cygpath -m dist` → `dist`），
  于是那个相对路径在 `cd` 之后被再拼一次 cwd，归档落到下一层。
  症状很隐蔽：脚本照样打印「已生成」，只有最后那句 `du` 以 cannot access 收场。
  CI 与 README 传的都是绝对路径，所以一直没暴露。现在**先绝对化、再 Windows 化**。
- **帮助窗口截图补上自证输出**：`BATCHSMITH_HELP_SHOT` 现在会打印章节数，以及
  「工具函数 N 个，分 M 组：…」。这张图最容易在"新增一个函数分组"时漏掉 ——
  分组排在正文里、一屏之外，肉眼比对不出来；有了这行输出，生成截图的人不用看图
  也能确认图上该有的东西在。
- **Windows ASan 作业死在配置步：Git Bash 把 `/fsanitize=address` 改写成了路径**。
  新作业第一次跑（run #19）就是红的，而且红在**配置**步、不是构建步 —— 根因是
  Windows 上 bash 给**原生**程序传 `/x/y` 形状的参数时会做 POSIX→Windows 路径转换：
  实测 `-DCMAKE_CXX_FLAGS="/fsanitize=address /Zi"` 到手变成
  `-DCMAKE_CXX_FLAGS=<Git 安装根>/fsanitize=address /Zi`。标志成了垃圾之后，
  CMake 的**编译器探测**（它用 `CMAKE_CXX_FLAGS`）过不去，于是配置阶段就失败，
  离根因很远。仓库里其它 job 传的都是 preset 名与 `-D` 开关，没有 `/` 开头的参数值，
  所以这个坑一直没暴露。修法：配置步显式设 `MSYS_NO_PATHCONV=1` 与
  `MSYS2_ARG_CONV_EXCL='*'`（两个变量分别是 Git for Windows 与 MSYS2 的开关），
  并把 `QT_ROOT_DIR` 改成从**运行时**环境取（原先用 `${{ env.QT_ROOT_DIR }}` 是解析期
  展开，拿不到值只会换来一句离根因很远的 `find_package(Qt6)` 失败）。
  ⚠️ 本机 `MSYS_NO_PATHCONV` 默认是开着的（WorkBuddy 注入），所以本地根本复现不出来 ——
  验证只能用"探针程序打印自己的 argv"在**清掉这两个变量**后跑一遍。
- **`pad` 的「先分配后检查」逃逸面**（ADR-6 逃逸面 1）：`pad(s, width)` 原来是先
  `while` 补到 `width`、**最后**才比对单次字符串上限 —— 等检查生效时内存早就分配出去了，
  `pad('x', 1e15)` 会一路补到把进程撑死，而不是报一句「超过上限」。ADR-6 当初把这一面
  覆盖在 `string.rep` / `string.format` / `table.concat` 上，漏了 `pad`。
  现在**进循环之前**先拿 `width` 比上限（结果长度只会是 `max(文本长, width)`，
  先判它就够）。报错里的宽度用 Lua 自己的整型格式符 `%I` 而不是 `%d` + `static_cast<int>`：
  宽度可以远超 `int`，截断后消息里会印出一个与用户输入无关的数字。
- **Windows 上「每个用例之后校验堆」其实一直没在查堆**：`tests/main.cpp` 里非
  MSVC-Debug 的那一路传的是 `GetProcessHeap()`，而 `/MD` 的 UCRT 与 MinGW 用的
  msvcrt 都**有自己的私有堆** —— `malloc`/`free` 走的是它，进程默认堆始终是干净的。
  于是 Release 构建上这条检查永远返回 TRUE：CI 的 Windows x64 作业从来没打印过
  `[heap]` 行，**不是堆干净，是没查对堆**。现在枚举进程内所有堆逐个校验。
  ⚠️ 但也别因此相信这一路：本机实测（`.workbuddy/tools/heap-probe/`）连**故意踩坏块头**
  都照样让 `HeapValidate` 返回 TRUE（现代堆默认启用 LFH，全量结构校验基本失效）。
  想要真证据只有两条路：Debug 的 CRT 调试堆，或 ASan / PageHeap。
- **`windows-heapcheck` 作业的「沉默」不再等于「干净」**：那一步原先无论结果如何都
  `exit 0`，而且一切正常时一条注解都不发 —— 于是"没有 `[heap]` 注解"既可能是堆没问题，
  也可能是这一步**什么都没跑到**（exe 路径写错 ⇒ 退出码 127 ⇒ 用例列表为空 ⇒ 照样绿），
  而两者的区别只存在于匿名读不到的 job 日志里。现在正常时也发一条正向 `::warning::`
  （整跑退出码 + 用例数 + 异常退出数），一个用例都没跑到就直接失败。

## [0.3.0] — 2026-09-15

**Phase 2 完成：DSL 编译器 + Lua 沙箱 + helper 集合。** 产品的核心（表达式求值）真正跑起来了。

### 新增

- **DSL 编译器** `dsl::compile_template`：模板 → Lua 源码，**编译一次产出可复用的函数**；
  区段包在括号里截断为单值、字面段用自定义单行转义器（不用 `string.format("%q")`）、
  区段错误能定位到具体哪一段。
- **沙箱** `sandbox::Sandbox`：ADR-6 的四重限制齐备 ——
  `_ENV` 白名单（含基础库白名单子集）、`lua_sethook` 指令数钩子、`lua_setallocf` 内存上限、
  复用同一钩子的墙钟超时；**违规在宿主侧打独立标记**，脚本 `pcall` 吞不掉。
- **helper 集合**（技术方案 §3.5 六类）：`count/index/tolist/slice/concat/sort/natsort/uniq`、
  `matrix/zip`、`seq/rand/seed/regex`、`fmt/upper/lower/trim/replace/match/pad`、
  `basename/dirname/ext/stem/join`、`num/str`；`i` / `rows` 由 env 注入。
  `math.random`/`randomseed` 换成沙箱自己的**可复现**随机源。
- **`bs eval`**：只跑编译与求值、**不接触文件系统**，输出逐行结果；`--show-lua` 可看编译产物。
- **列表缺省方式**：`Empty` / `Ignore`（整批取最短）/ `Repeat`（越界从头重复），
  越界一律返回空串而不是 nil。

### 变更

- 版本 **0.2.0 → 0.3.0**。
- **求值模型修正**（技术方案 §3.2）：模板若不用行上下文（不读 `i` / `rows`）就**只求值一次**。
  这是构想书示例 2（`$matrix(...)$` 必须 9 行）逼出来的结论；
  于是「逐行对应」与「整体展开」的歧义消失 —— 要逐行就写 `$list1[i]$`。
- 编译出的函数**不带参数**，`i` / `rows` 改由 env 注入（这样沙箱才能精确探测模板是否用到行上下文）。
- 删除 0.2.0 引入的临时求值器 `dsl::evaluate_simple`（它当初就是为了让界面能演示这条链路）；
  其单测演化为引擎测试，**构想书 6 个示例逐字对上**（`tests/core/test_engine.cpp`）。

### 修复

- 打错的列表名（`$list9$`）过去会被当成"未定义全局名 → nil → 空串"**静默通过**；
  现在明确报错并列出可用列表名，且指到具体区段。


### 待定（需要拍板，尚未实施）

- **多列表区段的默认展开规则**。当前按技术方案 §3.2 取**笛卡尔积**
  （`$list1$-$list2$` → 3×3 = 9 行）；已提出改为**默认逐行对应（zip）**
  （`$list1$-$list2$` → 3 行，短列表按空串补位）。**改动暂缓，结论未定**。
  若要改，涉及技术方案 §3.2（标注为「定稿」）、`docs/界面说明.md`、
  `dsl::evaluate_simple` 及其两处测试；笛卡尔积则改为只经 `matrix()` helper 提供。

## [0.2.0] — 2026-09-14

主界面落地，并打通 core 的模板扫描与求值链路。

### 新增

- **主界面（上下两段）**：上半是水平滚动的列表区——N 列并排、每列内部竖向滚动、
  列可增删（`＋ 添加列表` / 列标题右侧 `×`）；下半是单行表达式输入 + 右侧「确定」，
  其下是输出列表。
- **`batchsmith_gui` 静态库**：界面代码从可执行文件里拆出来，使其能被测试链接；
  `batchsmith` 只剩一个瘦 `main.cpp`。
- **core 求值链路**：`list/ListSource`（列表源）、`dsl/template_scanner`
  （`$...$` 分段、`\$` 转义、未配对 `$` 报错、失败时不返回半截结果）、
  `dsl/simple_evaluator`（**临时求值器**，见「说明」）。
- **GUI 离屏测试**：8 用例 / 76 断言，`QT_QPA_PLATFORM=offscreen`，覆盖主窗口装配、
  增删列与编号规则、回车与点「确定」等价、报错时清空输出。
- **文档与截图**：[`docs/界面说明.md`](docs/界面说明.md)（版面、交互契约、三条自定规则）、
  `docs/images/ui-main.png` 等界面截图。
- **截图工具**：`BATCHSMITH_UI_SHOT=<path>` 可用测试目标把界面渲染成 PNG
  （位于 `tests/`，不给发布产物增加只服务于截图的开关）。

### 变更

- 版本 **0.1.0 → 0.2.0**。
- GUI 测试单独一个目标：`batchsmith_tests` 仍只链 core（无图形依赖，可在无显示服务
  的环境跑）。
- `.gitattributes`：`third_party/**` 的 `-diff` / `linguist-vendored` 收窄到四个
  submodule 目录，不再误伤我们自己写的 `third_party/CMakeLists.txt` 与 `README.md`。

### 修复

- **macOS 链接失败 `ld: framework 'AGL' not found`**：Qt 6.9 之前，非商业发行版自带的
  `FindWrapOpenGL.cmake` 会把已从 macOS 26（Tahoe）SDK 移除的 AGL 写进 `Qt6::Gui`
  的公开链接接口。现以项目内的 `cmake/FindWrapOpenGL.cmake` 覆盖该模块
  （仅删 AGL 段，与上游 6.9 的修复逐行对应），并加配置期断言兜住模块搜索顺序这个前提。
- **CI：Windows 的 Qt arch** 改为 `win64_msvc2019_64`——Qt 6.7.2 的 Windows 包不存在
  `win64_msvc2022_64`（该后缀 6.8 才出现），原来的写法在下载 Qt 之前就失败。
- **Lua 静态库排除 `ltests.c`**：它是 `lua/lua` 镜像仓库比官方 tarball 多带的内部测试
  模块，引用了 `LUA_DEBUG` 下才编译的符号。编译单元 34 → 32。
- **打包脚本**不再假设 `windeployqt` / `macdeployqt` 在 `PATH` 上：优先用
  `install-qt-action` 显式导出的 `QT_ROOT_DIR`，再回退 `PATH`，两路都断则带提示退出。
- **CI artifact** 只上传打包好的归档，不再把「暂存目录 + 归档」两份重复内容一起上传
  （Windows artifact 实测 36.5MB → 与 zip 同量级）。

### 说明：`dsl::evaluate_simple` 是临时实现

它只支持「区段里写列表名」这一子集（`$list1$`、`mv $list1$ out`）；
helper、索引（`$list1[1]$`）、`#list1`、`$i$` / `$rows$` 一律**明确报错**并指向 Phase 2。

理由是技术方案 ADR-5 定的是**单运行时**：现在就让 core 长出一个自成一套语义的求值器，
正是 ADR-5 想避免的双语义漂移。它存在的目的只是让界面能演示「输入 → 输出」这条链路。
**Phase 2 的 Lua 编译器落地时必须删除它**，并让它现在这组用例继续通过——
那组用例同样是「行展开规则」的规格说明。

## [0.1.0] — 2026-09-13

P1（POC / 技术验证）完成。目标是先证明三件事：技术栈能在三平台跑通、产物体积可接受、
Lua 沙箱所需原语确实可用。

### 新增

- 工程骨架：CMake + Qt 6 Widgets + 四个 submodule 依赖（Lua 5.4 / sol2 / toml++ / doctest）
- 三个构建目标：`batchsmith_core`（只依赖 QtCore）、`bs`（CLI）、`batchsmith`（GUI）
- 构建链路：CMakePresets（本机与 CI 各一套）、Debug/Release
- 测试链路：doctest + ctest（10 用例 / 246 断言）
- CI：三平台矩阵（构建 + 测试 + 打包 + artifact），打 `v*` tag 自动发 Release
- 打包脚本：`packaging/package-{windows,macos,linux}.sh`
- 首个真实功能：自然排序比较器 `natural_compare`（`file2` 排在 `file10` 之前）
- 文档：技术方案与实现路线（ADR-1–10、DSL 规格）、项目阶段规划、Phase 1 施工记录

> 本文件不引用 git tag 链接：项目尚未打任何 tag。等真正发版打上 `v*` 之后，
> 再把这里的版本号补成可点击的 compare 链接。
