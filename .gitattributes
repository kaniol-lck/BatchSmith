# 默认把所有文本文件按 LF 存进仓库，检出时也用 LF。
# 三个平台的编译器都接受 LF，统一 LF 可以避免跨平台 diff 噪音。
* text=auto eol=lf

# 只有确实要求 CRLF 的格式才例外
*.bat    text eol=crlf
*.cmd    text eol=crlf
*.ps1    text eol=crlf
*.sln    text eol=crlf
*.vcxproj text eol=crlf
*.vcxproj.filters text eol=crlf

# 二进制
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.icns binary
*.qm   binary
*.woff binary
*.woff2 binary

# 第三方依赖不参与 GitHub 语言统计，也不在 diff 里展开。
# 只针对 submodule 目录 —— third_party/ 下另外两个文件（CMakeLists.txt、README.md）
# 是我们自己写的，必须能正常 diff、能进语言统计。
third_party/lua/**          linguist-vendored -diff
third_party/sol2/**         linguist-vendored -diff
third_party/tomlplusplus/** linguist-vendored -diff
third_party/doctest/**      linguist-vendored -diff

# 生成物不参与语言统计
*.in linguist-language=Text
